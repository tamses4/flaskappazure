# ================================================================
# app.conf - Configuration Backend
# Déployé automatiquement dans /etc/monapp/app.conf
# ================================================================

[server]
host = 0.0.0.0
port = 5000

[logging]
level = INFO
file = /var/log/monapp/app.log

[health]
endpoint = /health
